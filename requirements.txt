# local package
-e .

# external requirements
click
Sphinx
coverage
awscli
flake8
python-dotenv>=0.5.1
# Package requirements for the Bank Marketing project
# ------------------------------------------------

# Core data science stack
pandas>=2.0
numpy>=1.21

# Visualization
matplotlib>=3.7
seaborn>=0.12

# Jupyter (for notebooks)
jupyter>=1.0
jupyterlab>=4.0  # optional but nicer than classic notebook

# Optional: nice progress bars & CLI (you already have click)
tqdm
rich

# Keep the original CCDS dev tools (important for linting/tests)
click
Sphinx
coverage
awscli
flake8
python-dotenv>=0.5.1

# If you ever want to freeze exact versions later:
# pip freeze > requirements.txt